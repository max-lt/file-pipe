# file-pipe

HTTP pipe service — stream uploads to downloads in real-time.

## Build & test

```
cargo build
cargo test
```

## Workflow rules

- Before fixing a bug, always write a regression test first that reproduces the issue, then fix the code to make it pass.
- We use bun as package manager (for any JS tooling).
